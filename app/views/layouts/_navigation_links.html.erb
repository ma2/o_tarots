<%# add navigation links to this file %>
<li class="dropdown">
  <a href="#" class="dropdown-toggle" data-toggle="dropdown">タロット <b class="caret"></b></a>
  <ul class="dropdown-menu">
    <li><%= link_to '偶然タロット', play_random_tarots_path %></li>
    <% Tarot.all.each do |tarot| %>
        <li><%= link_to tarot.name, play_tarot_cards_path(tarot) %></li>
    <% end %>
  </ul>
</li>

