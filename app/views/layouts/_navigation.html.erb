<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Home', root_path, class: 'navbar-brand' %>
      <% if @play_mode %>
          <%= link_to 'カード', draw_one_tarot_cards_path(@tarot), class: 'navbar-brand draw-card', data: {remote: true, disable_with: '...'} %>
          <%= link_to 'シャッフル', play_tarot_cards_path(@tarot), class: 'navbar-brand' %>
      <% end %>
      <% if @play_random_mode %>
          <%= link_to 'カード', draw_random_one_tarots_path , class: 'navbar-brand draw-card', data: {remote: true, disable_with: '...'} %>
          <%= link_to 'シャッフル', play_random_tarots_path , class: 'navbar-brand' %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
    </div>
  </div>
</nav>
